# Create neo4j.env file and edit it in vim editor
touch neo4j.env
vi kafka.env

# Create folder for storing Kafka data
mkdir ~/kafka

# Create docker-compose.yml file
touch docker-compose.yml
vi docker-compose.yml

# Create template-docker-compose file
touch template-docker-compose
vi template-docker-compose